# Music Files

Place your `.mp3` or `.flac` files here for the music player.

The default playlist in `components/interactive/MusicPlayer.tsx` references `example.mp3`.
Rename your file accordingly or update the playlist array in the component.

> Audio files are excluded from git via `.gitignore`.
